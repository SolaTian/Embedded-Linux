    # --------------------------
    # 私有协议（结构体+数据）
    # --------------------------
    [Application.CustomProtocol]
    enabled = false
    listen_port = 9000
    data_parser = /opt/parsers/custom_protocol.lua   # 自定义Lua解析脚本路径
    binary_format = true           # 是否使用二进制传输
    response_template = /opt/templates/custom_response.json